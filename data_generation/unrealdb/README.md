# unrealdb

A python library to use unrealcv and blender to generate synthetic data.

Use `pip install -e .` first, so that you can `import unrealdb as udb` in your code.

This library will be available in `pip` soon.
